---
# Main orchestration for asdf role
# Handles installation and shell configuration only
# Plugin installation, versions, and default packages are managed via dotfiles

- name: Install asdf
  include_tasks: install.yml
  tags: ['asdf', 'asdf:install']

- name: Configure shell initialization
  include_tasks: configure-shell.yml
  tags: ['asdf', 'asdf:shell']
