# Node.js default packages - installed automatically by asdf when installing Node.js versions
# Base packages (always installed)
{% for pkg in default_npm_base %}
{{ pkg }}
{% endfor %}

# Optional packages (from playbook variables)
{% if default_npm_optional is defined %}
{% for pkg in default_npm_optional %}
{{ pkg }}
{% endfor %}
{% endif %}
