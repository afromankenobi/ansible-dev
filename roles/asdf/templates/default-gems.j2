# Ruby default gems - installed automatically by asdf when installing Ruby versions
# Base gems (always installed)
{% for gem in default_gems_base %}
{{ gem }}
{% endfor %}

# Optional gems (from playbook variables)
{% if default_gems_optional is defined %}
{% for gem in default_gems_optional %}
{{ gem }}
{% endfor %}
{% endif %}
