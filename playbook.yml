---
- name: Provision dev server
  become: true
  hosts: all

  roles:
    - base # packages used for everything else
    - postgresql
    - yarn
    - rbenv
    - zeal
    - dotfiles
    - neovim
    - ohmyzsh
    - virtualbox
